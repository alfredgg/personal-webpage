<h1 class="content-subhead">Volunteer</h1>
{{#each volunteer}}
  <div class="section">
    <h2 class="section-title">{{position}} at {{organization}}</h2>
    <div>
        {{#if website}}<a href="{{website}}">{{website}}</a>{{/if}}
        <span class="additional-info">{{startDate}} - {{#if endDate}}{{endDate}}{{else}}Nowadays{{/if}}</span>
    </div>
    <p>{{summary}}</p>
 </div>
{{/each}}